# Fiochat unattended install answers file
#
# Usage:
#   ./install.sh validate --answers /etc/fiochat/install.env --mode production --yes
#   ./install.sh apply    --answers /etc/fiochat/install.env --mode production --yes
#   ./install.sh verify

# ----------------------------------------------------------------------------
# Installer behavior
# ----------------------------------------------------------------------------
FIOCHAT_INSTALL_MODE=production
FIOCHAT_INSTALL_METHOD=release
FIOCHAT_INSTALL_TAG=v0.2.0
FIOCHAT_INSTALL_CONFIG_SOURCE=rebuild
FIOCHAT_INSTALL_SERVICE_USER=svc
FIOCHAT_INSTALL_START_SERVICES=1

# Optional: override release repo owner/name (default: joon-aca/fiochat)
# FIOCHAT_INSTALL_REPO=joon-aca/fiochat

# ----------------------------------------------------------------------------
# AI provider configuration
# ----------------------------------------------------------------------------
FIOCHAT_PROVIDER=openai
FIOCHAT_MODEL=gpt-4o-mini
FIOCHAT_OPENAI_API_KEY=sk-REPLACE_ME

# Provider-specific alternatives (set only for chosen provider):
# FIOCHAT_CLAUDE_API_KEY=sk-ant-REPLACE_ME
# FIOCHAT_AZURE_API_BASE=https://YOUR_RESOURCE.openai.azure.com/
# FIOCHAT_AZURE_API_KEY=REPLACE_ME

# ----------------------------------------------------------------------------
# Telegram configuration
# ----------------------------------------------------------------------------
FIOCHAT_TELEGRAM_BOT_TOKEN=123456:REPLACE_ME
FIOCHAT_ALLOWED_USER_IDS=123456789
FIOCHAT_SERVER_NAME=my-server

# Optional telegram fields
# FIOCHAT_OPS_CHANNEL_ID=-1001234567890
# FIOCHAT_AI_SERVICE_API_URL=http://127.0.0.1:8000/v1/chat/completions
# FIOCHAT_AI_SERVICE_MODEL=default
# FIOCHAT_AI_SERVICE_AUTH_TOKEN=Bearer <no-auth>
